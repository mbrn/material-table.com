import LiveCode from '../../../../components/live-code';
import MaterialTable from 'material-table';

# Export

material-table has built-in export feature. It supports only csv format. 

## Usage

To make export available you must open exportButton

```jsx
import MaterialTable from 'material-table';

<MaterialTable
    // other props
    options={{
      exportButton: true
    }}
/>
```

<br />

## Hints 

* You can change export file name by setting `options.exportFileName`
* You can change csv delimeter by setting `options.exportDelimiter`

## Examples

<LiveCode
    code={require('!!raw-loader!./basic-export.js').default}
    scope={{ MaterialTable }}
    title="Basic Export Example"
    description="To make export available options.exportButtons is being set as true."
/>

<LiveCode
    code={require('!!raw-loader!./non-export-field.js').default}
    scope={{ MaterialTable }}
    title="Non Export Field Example"
    description="name field marked as non-export by setting export field of specified column as false."
/>

<LiveCode
    code={require('!!raw-loader!./overriding-export-function.js').default}
    scope={{ MaterialTable }}
    title="Overriding Export Function Example"
    description="Export function of table can be override by setting options.exportCsv"
/>


